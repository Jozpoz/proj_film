#include "director.h"
#include "actor.h"
#include "film.h"
#include <iostream>
#include <stdio.h>
#include <time.h>
#include <unistd.h>
#include <stdlib.h>
#include <vector>

#define AMOUNT_OF_DIRECTORS 20
#define AMOUNF_OF_ACTORS 120

Dir poolOfDirectors[AMOUNT_OF_DIRECTORS];
Act poolOfActors[AMOUNF_OF_ACTORS];

Film filmVector[104];
Film filmWeeklyWinner[52];

void fiftyTwoWeeks();
{
	int k = 0;
	while(k < 52)		//symulacja 52 tygodni
		{
			int i = 0;
			int j = 1;
			

		if (filmVector.box_office[i] <= filmVector.box_office[j]) {filmVector.box_office[i] = filmWeeklyWinner.[k]}

			i+2;
			j+2;
			k++;
		 
		}
}


int main (int argc, char constant argv[])
{
	vector < film > filmVector;
	vector < film > filmWeeklyWinner;

while(1)
{
		int k = 0;
		
}
	srand(argc == )
}
}